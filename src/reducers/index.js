const initialState = {
  isFetching: false,
  Messages: [],
  error: '',
};

export const messagesReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
